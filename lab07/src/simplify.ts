import { Expr } from "../../lab04";

export function simplify(e: Expr, identities: [Expr, Expr][]): Expr
{
    return e;
    // let result: Expr;
    // switch(e.type) {
    //     case "num":

    //     case "var":

    //     case "neg":

    //     case "bin":
            
    // }

    // return result;
}